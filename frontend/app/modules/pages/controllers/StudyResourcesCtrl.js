define(['app'], function(app) {
    app.controller('Pages.StudyResourcesCtrl',
        ['$scope', '$routeSegment', '$location',
            function ($scope, $routeSegment, $location) {
            }]);
});